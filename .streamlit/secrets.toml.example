# Copy this file to .streamlit/secrets.toml for local development,
# or paste the same values into Streamlit Community Cloud -> App settings -> Secrets.

# Preferred setup: SSH tunnel + DB credentials
ssh_host = "129.187.44.36"
ssh_port = 443
ssh_user = "societalcomputing"
ssh_password = "YOUR_SSH_PASSWORD"

db_host = "127.0.0.1"
db_port = 3306
db_user = "aligniverse_team"
db_password = "YOUR_DB_PASSWORD"
db_name = "deepfakes"

# Optional fallback (supported by app.py):
# DB_URL = "mysql+mysqlconnector://USERNAME:PASSWORD@HOST:3306/DATABASE_NAME"